#include "gtest/gtest.h"
#include <cmath>

// definition of the functionality to be tested
// for production code, we use header files in the ../include directory
double squareRoot (const double & a);
